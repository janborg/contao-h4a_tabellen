{% extends '@Contao/block_searchable' %}

{% block content %}

    <div id='{{ containerId }}'></div>

<script>
    _hb({
        widget: '{{ widget_type == 'tabelle' ? 'tabelle' : 'spielplan' }}',
        {{ widget_type == 'club' ?  'clubId' : 'teamId' }}: '{{ teamId }}', 
        container: '{{ containerId }}'
    });
</script>

{% endblock %}
